"""Async v2 client tests colocated with async implementation modules.

Each test module mirrors the async client file it exercises so sync and async
coverage can evolve alongside their respective implementations.
"""
