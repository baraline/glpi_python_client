"""Tests for the hand-written concurrency primitives.

``_concurrency`` is the one module maintained by hand on both sides, so
its tests are too: each surface asserts what is true of its own
primitives rather than a transformed copy of the other's.
"""
